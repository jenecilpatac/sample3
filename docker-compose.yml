version: '3.8'

services:
  web:
    build: .
    ports:
      - "5003:5003"
    volumes:
      - .:/usr/share/nginx/html
    container_name: sample2
    networks:
      - webnet

networks:
  webnet:
    driver: bridge
